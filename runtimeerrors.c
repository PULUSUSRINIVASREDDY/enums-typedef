#include<stdio.h>
enum {ap,ts,mp,bihar,jk,hp};  // without variable name what the output//->it comes output but not mandaratory  
int main()
{
	printf("%d,%d,%d,%d,%d,%d",ap,ts,mp,bihar,jk,hp);

	return 0;
}
