#include<stdio.h>
enum  state{ap,ts,mp,bihar,jk,hp}; 
int main()
{
       // printf("%d,%d,%d,%d,%d,%d",ap,ts,mp,bihar,jk,hp);

	// we did'nt get any output or runtime errors or errors
        return 0;
}

